//check the guilds that the client is in
// place them into an array and then iterate through them place id in a string and then get another array of names of the folders in the guilds folder and see if names match up
//if they dont create a new folder and run the setup
// else run the monitor function
// on the guild create event add the guild to the guilds folder and the config and the monitor